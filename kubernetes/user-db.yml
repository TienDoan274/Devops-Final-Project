apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-db
  template:
    metadata:
      labels:
        app: user-db
    spec:
      containers:
      - name: user-db
        image: mysql:8
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: pfm_dc_2020
        - name: MYSQL_DATABASE
          value: user
        - name: MYSQL_USER
          value: cloudacademy
        - name: MYSQL_PASSWORD
          value: pfm_2020
        volumeMounts:
        - name: userdb-storage
          mountPath: /var/lib/mysql
      volumes:
      - name: userdb-storage
        persistentVolumeClaim:
          claimName: userdb-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: user-db
spec:
  ports:
    - port: 3306
  selector:
    app: user-db
  type: LoadBalancer
